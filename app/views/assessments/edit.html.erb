<h1>Editing Assessment</h1>
<%= form_for(@assessment,:url => student_assessment_path(@student_id,@assessment.id)) do |f| %>
  <% if @assessment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assessment.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% @assessment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :FA1 %><br>
    <%= f.text_field :FA1 %>
  </div>  <div class="field">
    <%= f.label :FA2 %><br>
    <%= f.text_field :FA2 %>
  </div>  <div class="field">
    <%= f.label :SA1 %><br>
    <%= f.text_field :SA1 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Show', student_assessment_path(@assessment.student_id,@assessment.id) %> |
<%= link_to 'Back', students_path %>
